<a href="/songs/new">Post a song</a>

<% @songs.each do |song| %>
    <div class="song">
      <h3>
        <a href="/songs/<%= song.id %>"><%= song.title %></a>
        <small>
          <%= User.find(song.user_id).email %>
        </small>
      </h3>
      <p>
        <a href="<%= song.url %>"><%= song.url %></a>
      </p>
    </div>
<% end %>
